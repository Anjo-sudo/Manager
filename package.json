{
  "name": "synctask-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && DATABASE_URL=\"file:./data.db\" npx prisma generate",
    "start": "cd backend && DATABASE_URL=\"file:./data.db\" npx prisma db push && DATABASE_URL=\"file:./data.db\" node server.js"
  }
}
